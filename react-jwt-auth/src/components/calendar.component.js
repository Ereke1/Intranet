// components/Calendar.js
import React from 'react';

const Calendar = ({ user }) => {
    return (
        <section className="calendar">
            <h2>Календарь</h2>

        </section>
    );
};

export default Calendar;